# @fohte scoped packages (e.g. @fohte/blog-publisher-contract) are published to
# GitHub Packages. Installing them requires a token with the `read:packages`
# scope exposed as NODE_AUTH_TOKEN.
@fohte:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
