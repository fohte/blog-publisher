# Template: replace the placeholders with real values and apply via
#   kubectl create secret generic blog-publisher-secret --from-env-file=.env -n <ns>
# or commit a sealed/sops-encrypted variant. The plain manifest below is reference only.
apiVersion: v1
kind: Secret
metadata:
  name: blog-publisher-secret
  labels:
    app: blog-publisher
type: Opaque
stringData:
  BEARER_TOKEN: REPLACE_ME
  COUCHDB_URL: http://couchdb.livesync.svc.cluster.local:5984
  COUCHDB_USERNAME: REPLACE_ME
  COUCHDB_PASSWORD: REPLACE_ME
  COUCHDB_DATABASE: REPLACE_ME
  LIVESYNC_PASSPHRASE: REPLACE_ME
  GITHUB_APP_ID: REPLACE_ME
  GITHUB_APP_PRIVATE_KEY: |
    -----BEGIN PRIVATE KEY-----
    REPLACE_ME
    -----END PRIVATE KEY-----
  GITHUB_APP_INSTALLATION_ID: REPLACE_ME
  R2_BUCKET: REPLACE_ME
  R2_ACCOUNT_ID: REPLACE_ME
  R2_ACCESS_KEY_ID: REPLACE_ME
  R2_SECRET_ACCESS_KEY: REPLACE_ME
